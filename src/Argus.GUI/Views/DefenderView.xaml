<UserControl x:Class="Argus.GUI.Views.DefenderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:Argus.GUI.ViewModels">

    <ScrollViewer Style="{StaticResource DarkScrollViewer}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Page header -->
            <StackPanel Grid.Row="0">
                <TextBlock Text="DEFENDER" Style="{StaticResource CinzelSectionTitle}"/>
                <TextBlock Text="Real-time protection activity log"
                           FontFamily="{StaticResource FontBarlow}"
                           FontSize="13"
                           Foreground="{StaticResource TextSecondary}"
                           Margin="0 6 0 0"/>
                <Rectangle Height="2" Margin="0 14 0 18" Fill="{StaticResource GoldRule}"/>
            </StackPanel>

            <!-- Filter pills -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0 0 0 12">
                <Border Background="{StaticResource BgBadgeGreen}" Padding="12 5" Margin="0 0 8 0">
                    <TextBlock Text="ALL"
                               FontFamily="{StaticResource FontBarlowCondensed}"
                               FontSize="11"
                               FontWeight="Medium"
                               Foreground="{StaticResource TextBadgeGreen}"/>
                </Border>
                <Border Background="{StaticResource BgBadgeRed}" Padding="12 5" Margin="0 0 8 0">
                    <TextBlock Text="MALICIOUS"
                               FontFamily="{StaticResource FontBarlowCondensed}"
                               FontSize="11"
                               FontWeight="Medium"
                               Foreground="{StaticResource TextBadgeRed}"/>
                </Border>
                <Border Background="{StaticResource BgBadgeGold}" Padding="12 5" Margin="0 0 8 0">
                    <TextBlock Text="SUSPICIOUS"
                               FontFamily="{StaticResource FontBarlowCondensed}"
                               FontSize="11"
                               FontWeight="Medium"
                               Foreground="{StaticResource TextBadgeGold}"/>
                </Border>
                <Border Background="{StaticResource BgBadgeGray}" Padding="12 5">
                    <TextBlock Text="CLEAN"
                               FontFamily="{StaticResource FontBarlowCondensed}"
                               FontSize="11"
                               FontWeight="Medium"
                               Foreground="{StaticResource TextBadgeGray}"/>
                </Border>
            </StackPanel>

            <!-- Activity DataGrid -->
            <DataGrid Grid.Row="2"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      Background="{StaticResource BgStatCard}"
                      BorderBrush="{StaticResource BorderDefault}"
                      BorderThickness="1"
                      Foreground="{StaticResource TextPrimary}"
                      RowBackground="{StaticResource BgStatCard}"
                      AlternatingRowBackground="{StaticResource BgSurface}"
                      GridLinesVisibility="Horizontal"
                      HeadersVisibility="Column"
                      HorizontalGridLinesBrush="{StaticResource BorderDefault}"
                      CanUserResizeRows="False"
                      CanUserSortColumns="True"
                      SelectionMode="Single"
                      SelectionUnit="FullRow">
                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="14 10"/>
                        <Setter Property="FontFamily" Value="{StaticResource FontBarlow}"/>
                        <Setter Property="FontSize" Value="12"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border BorderThickness="{TemplateBinding BorderThickness}"
                                            Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="BorderThickness" Value="3 0 0 0"/>
                                <Setter Property="BorderBrush" Value="{StaticResource AccentGold}"/>
                                <Setter Property="Background" Value="{StaticResource BgNavActive}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.CellStyle>
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="{StaticResource BgSurface}"/>
                        <Setter Property="Foreground" Value="{StaticResource AccentGold}"/>
                        <Setter Property="FontFamily" Value="{StaticResource FontBarlowCondensed}"/>
                        <Setter Property="FontWeight" Value="Medium"/>
                        <Setter Property="FontSize" Value="11"/>
                        <Setter Property="Padding" Value="14 10"/>
                        <Setter Property="BorderBrush" Value="{StaticResource AccentGold}"/>
                        <Setter Property="BorderThickness" Value="0 0 0 1"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="TIME"          Binding="{Binding Time}"        Width="120"/>
                    <DataGridTextColumn Header="TYPE"          Binding="{Binding Type}"        Width="120"/>
                    <DataGridTextColumn Header="EVENT"         Binding="{Binding Event}"       Width="*"/>
                    <DataGridTextColumn Header="THREAT LEVEL"  Binding="{Binding ThreatLevel}" Width="140"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </ScrollViewer>
</UserControl>
